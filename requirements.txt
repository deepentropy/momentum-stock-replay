# Databento API client (recommended)
databento>=0.40.0

# IB Gateway API client (legacy - for backup)
ib_async==2.0.1

# Data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0

# HTTP requests (for GitHub API)
requests>=2.31.0
